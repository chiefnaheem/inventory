{
    "name": "tiny-inventory",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
        "install:all": "npm install --prefix server && npm install --prefix web",
        "build": "npm run build --prefix server && npm run build --prefix web",
        "start": "npx concurrently \"npm run start --prefix server\" \"npm run preview --prefix web\""
    }
}